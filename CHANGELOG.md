# Changelog — ERDL Decision Object Test Vectors

> Copyright © 2026 深圳市秒镜科技有限公司 (Shenzhen Miaojing Technology Co., Ltd.) · vectors & spec CC0-1.0 · code Apache-2.0

> **License change (2026-09-01)**: MIT → Apache-2.0 (explicit patent grant).
>
> **License split (2026-09-04)**: two-tier license — test vectors + spec docs → CC0-1.0; code → Apache-2.0.

## v1.6.0 (current) - 2026-09-09

### Changed
- **Expression layer 239 → 240 vectors** (Core 317 → 318): added an E10 in-membership NFC vector.
- **ER3 result-object format** (`value`/`value_type` ∈ number/string/boolean): fold internal types (rational/date/undefined/null/object) to ER3; numbers serialized as decimal strings (RFC 8785 §3.1); `errored` semantics specified (E3 evaluation errors → `true`).
- **gloss/projection expected values English-only** (spec §5.5 English canonical); GLOSS / INTEGRITY / PROJ expected now ER3 `value`/`value_type`.
- **warning semantics unified**: comparison / between type-mismatch → silent false (no warning); string / in / length / aggregate type-mismatch → `type_mismatch` warning + `errored: false`; quantifier over a missing field → silent false (E11).

### Added
- **expression-layer submission pipeline**: `verify-v-engine-submission.mjs` (ER3 envelope cross-verification) + `update-expression-registry.cjs` + expression-layer registry in IMPLEMENTATIONS.md + CI (layer-dispatched cross-verify + auto-record).

### Fixed
- **rate modifier truth value inverted** (SPEC §5.2: over-limit → true).
- **`in` membership comparison NFC-normalized** (E10).

## v1.5.1 (current) - 2026-09-06

### Added
- Null-check asymmetry vectors (expression layer 236 → 239): lock `== null`/`!= null` field-presence sensing semantics.

### Fixed
- V-GLOSS vector `node` field normalized to concrete node names (added `node_group`).

### Changed
- Spec field `erdl-spec-v2.0` → `v2.1`; stale spec link fixed; RFC-002 cross-refs mapped.
- Added badges, POC-welcome note and support contact.

## v1.5.0

Decision Object v1.5 flat hash-chain cross-implementation test vectors.

- **V-DO-v15 audit layer 78 vectors** (`decision-object-vectors-v1.5.json`): decision types 13 / chain-attack detection 8 / anchoring-attack detection 10 / canary 1 / conclusion layer 14 / jurisdiction compliance 32
- **V-ENGINE expression layer 236 vectors** (`v-engine-vectors.json`): node semantics 136 (34 nodes × 4 scenarios) / evaluation constraints 48 / Simple compilation 30 / gloss 16 / projection facets 6
- **Normative contract**: `RUNNER_CONTRACT.md` (rules R1–R6) + `docs/VERIFIER-GUIDE.md`
- **Verification program**: five-step verification Step 0–6 (RFC-002 §7), reference implementation `scripts/verify-v1.5.js` (zero-dependency self-built JCS)
- **Automatic recording**: `scripts/generate-conformance.cjs` → `conformance/CONFORMANCE.md` (generated by CI, recording Check 1/2 + K01 discrimination + R1–R6; `npm run conformance`) + `submissions/README.md` (third-party runner submission pipeline)
- **2026-09-06 increment (non-DO version bump, Core 311 → 314)**: added 3 E11 type-mismatch constraint vectors locking the G4 semantics — `ne`/`eq` between mixed non-null types (bool vs number, string vs number) folds **false** for both eq and ne (SPEC §7.3(a), no fail-open via JS `!==`). Expression layer 233 → 236, semantic-sensitive vectors 71 → 74; the independent verifier now recomputes type-mismatch folding. Engine fix mirrored across erdl-landing / rulsynor-core / rulsynor backend.
- **2026-09-06 increment (non-DO version bump, Core 305 → 311)**: added 6 constraint vectors locking the G1/G2/G3 semantics — `== null`/`!= null` field-presence sensing (E11 ×3), `min`/`max` empty-array safe-folding to false (E8 ×2), and `date_add` non-integer amount rejection (E9 ×1, §7.3(f) amount MUST integer). Expression layer 227 → 233, semantic-sensitive vectors 61 → 71; the independent verifier (`verify-v-engine.mjs`) now recomputes E11 null-propagation + `date_add` integer rejection.
- **2026-09-05 increment (non-DO version bump, Core 301 → 305)**: added 4 E9 time-node UTC semantics constraint vectors (`epoch_ms` no-timezone suffix = UTC / `Z` suffix / `+08:00` offset / `days_between` floor), locking the spec §7.3(f) "no-timezone suffix = UTC" cross-implementation semantics; expression layer 223 → 227, semantic-sensitive vectors 57 → 61. Also fixed the independent verifier (`verify-v-engine.mjs`) time parsing: strict ISO 8601 (no-timezone suffix appends Z → UTC) + `days_between` `Math.round` → `Math.floor` (aligned with §7.3(f)).
- **2026-09-02 increment (non-DO version bump, Core 301 unchanged)**: added §1.4 production-side invariant / §1.5 decision-derivation semantics / §1.6 Producer Contract; added decision_divergence (cross-layer semantic re-derivation, V-DIVERGENCE 3 vectors, `npm run verify:decision`) + V-PRODUCER (producer-side conformance, `npm run verify:producer`); added P-05 residual risk to Appendix A; P6 resolvable-set semantic clarification. Thanks: Santosh Kumar Puppala (norviq-dev).

## v1.3 (historical archive, archived in archive/v1.3/)

Decision Object v1.3, 13 AV vectors, byte-verified by Erik Newton (Concordia) as an independent Runner.
